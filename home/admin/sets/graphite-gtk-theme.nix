# This is an argset for configuring the graphite-gtk-theme
# https://github.com/vinceliuice/Graphite-gtk-theme
{
  # colorVariants = [ "all" ];
  # grubScreens   = [ ];
  # sizeVariants  = [ "standard" ];
  # themeVariants = [ "blue" ];
  # tweaks        = [ ];
  # wallpapers    = false;
  # withGrub      = false;
}
